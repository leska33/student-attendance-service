# Railway waits for HTTP 200 on this path before marking the deploy healthy.
# Aggregate /actuator/health can return 503 when any contributor (e.g. DB) is DOWN.
# Liveness only reflects that the Spring context is up — fix DB vars so the app actually works.
[deploy]
healthcheckPath = "/actuator/health/liveness"
healthcheckTimeout = 300
