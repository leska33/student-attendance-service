# Railway waits for HTTP 200 on this path before marking the deploy healthy.
# Spring Boot 4 maps liveness to /livez on the *main* server port when
# management.endpoint.health.probes.add-additional-paths=true (see application.properties).
[deploy]
healthcheckPath = "/livez"
healthcheckTimeout = 300
